/**
 * Placeholder host info, matching the values used in the Onboarding
 * design. Replaced with a real platform query (e.g. via tauri-plugin-os
 * or a custom command) when the audit pipeline lands and the dashboard
 * needs the actual device identity.
 */
export const TARGET_MACHINE = {
  hostname: "ADMIN-WKS-042",
  osName: "Windows 11 Pro",
  osVersion: "24H2",
  osBuild: "26100.3194",
};
